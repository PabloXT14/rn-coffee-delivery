import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    marginTop: -108,
    zIndex: 2,
    height: 323,
    overflow: 'visible',
  },
  content: {
    alignItems: 'center',
    paddingHorizontal: 32,
    gap: 32,
  },
})
