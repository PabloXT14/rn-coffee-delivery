import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    zIndex: 2,
    overflow: 'visible',
    marginTop: -110,
  },
  content: {
    height: 323,
    alignItems: 'center',
    paddingHorizontal: 32,
    gap: 32,
  },
})
