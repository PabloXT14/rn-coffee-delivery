import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    marginTop: -118,
    zIndex: 2,
    height: 323,
  },
  content: {
    alignItems: 'center',
    paddingHorizontal: 32,
    gap: 32,
  },
})
